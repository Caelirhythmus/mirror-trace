import { defineConfig } from 'vite';

export default defineConfig({
  /* Use relative asset paths so the site works when deployed to
     any subdirectory path (GitHub Pages, custom domain, etc.). */
  base: './',
});
